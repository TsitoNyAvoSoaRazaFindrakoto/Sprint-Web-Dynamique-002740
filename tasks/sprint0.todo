Sprint 0 :
    FrontController:
        ☐ FrontController est un HttpServlet
        On cree une fonction processRequest() :
            ☐ elle sera appelee par `doPost()` et `doGet()`
            elle affichera l'url tape par le client :
                ☐ prendre le PrintWriter de la reponse
                ☐ prendre l'url de la requete
                ☐ afficher l'url avec l'aide du PrintWriter
    web.xml :
        Ajout d'un servlet "FrontServlet" :
            ☐ son url pattern sera '/'